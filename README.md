# jean-delfinado-dp

This repo exists only to keep an old link working.

The portfolio used to be served from here via GitHub Pages at
`jeanomorph.github.io/jean-delfinado-dp/`. It now lives on Vercel:

### → https://jean-delfinado.vercel.app/

`index.html` is a redirect, nothing else. Anyone landing on the old URL is sent
straight to the live site.

The actual source lives in a separate private repo. If the site ever moves again,
update the three URLs in `index.html` and push — that's the whole job.
